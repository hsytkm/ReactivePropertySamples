<UserControl x:Class="ReactivePropertySamples.Views.Controls.MyPageTitle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel>
        <TextBlock>
            <Run FontSize="{StaticResource FontSizeTitle}" Text="{Binding Title, Mode=OneTime}" />
            <Run Text="      " />
            <Run FontSize="{StaticResource FontSizeSubtitle}" Text="{Binding Subtitle, Mode=OneTime}" />
        </TextBlock>

        <TextBlock Margin="10,3" FontSize="{StaticResource FontSizeDescription}"
                   Text="{Binding Description, Mode=OneTime}" />

        <!--
        <TextBlock Margin="10,3" FontSize="{StaticResource FontSizeKeywords}"
                   Text="{Binding Keywords, Mode=OneTime, Converter={StaticResource StringConcatConverter}}" />
        -->
    </StackPanel>
</UserControl>
